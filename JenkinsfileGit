pipeline {
    agent any

    environment {
        // GitHub configuration
        GITHUB_TOKEN = credentials('github-token-actions')
        GITHUB_OWNER = 'SatriaBPY'
        GITHUB_REPO = 'Web-testing-demoblaze'
        GITHUB_API_URL = "https://api.github.com/repos/${GITHUB_OWNER}/${GITHUB_REPO}"
    }

    stages {
        stage('Trigger GitHub Actions Workflow') {
            steps {
                script {
                    echo "🚀 Triggering GitHub Actions workflow..."

                    // Trigger workflow via GitHub API
                    def response = sh(
                        script: """
                            curl -s -X POST \
                                -H "Accept: application/vnd.github+json" \
                                -H "Authorization: Bearer ${GITHUB_TOKEN}" \
                                ${GITHUB_API_URL}/actions/workflows/playwright-test.yml/dispatches \
                                -d '{"ref":"main", "inputs":{"branch":"main"}}'
                        """,
                        returnStdout: true
                    )

                    echo "✅ Workflow triggered successfully"
                    sleep time: 5, unit: 'SECONDS'  // Beri waktu workflow untuk start
                }
            }
        }

        stage('Wait for Workflow to Complete') {
            steps {
                script {
                    echo "⏳ Waiting for workflow to complete..."

                    // Loop sampai workflow selesai (max 30 menit)
                    def maxRetries = 60  // 30 menit (30 detik per retry)
                    def workflowCompleted = false
                    def runId = null

                    for (int i = 0; i < maxRetries; i++) {
                        // Ambil run ID terbaru untuk workflow ini
                        def runs = sh(
                            script: """
                                curl -s -H "Accept: application/vnd.github+json" \
                                    -H "Authorization: Bearer ${GITHUB_TOKEN}" \
                                    "${GITHUB_API_URL}/actions/workflows/playwright-test.yml/runs?per_page=1" \
                                    | jq -r '.workflow_runs[0].id // empty'
                            """,
                            returnStdout: true
                        ).trim()

                        if (runs) {
                            runId = runs

                            // Cek status workflow
                            def status = sh(
                                script: """
                                    curl -s -H "Accept: application/vnd.github+json" \
                                        -H "Authorization: Bearer ${GITHUB_TOKEN}" \
                                        "${GITHUB_API_URL}/actions/runs/${runId}" \
                                        | jq -r '.status'
                                """,
                                returnStdout: true
                            ).trim()

                            def conclusion = sh(
                                script: """
                                    curl -s -H "Accept: application/vnd.github+json" \
                                        -H "Authorization: Bearer ${GITHUB_TOKEN}" \
                                        "${GITHUB_API_URL}/actions/runs/${runId}" \
                                        | jq -r '.conclusion // "pending"'
                                """,
                                returnStdout: true
                            ).trim()

                            echo "Run ID: ${runId}, Status: ${status}, Conclusion: ${conclusion}"

                            if (status == "completed") {
                                workflowCompleted = true
                                echo "✅ Workflow completed with conclusion: ${conclusion}"
                                break
                            }
                        }

                        echo "Waiting... (${i+1}/${maxRetries})"
                        sleep time: 30, unit: 'SECONDS'
                    }

                    if (!workflowCompleted) {
                        error "Workflow did not complete within timeout"
                    }

                    // Simpan runId untuk stage berikutnya
                    env.GITHUB_RUN_ID = runId
                }
            }
        }

        stage('Download Allure Results') {
    steps {
        script {
            echo "📥 Downloading artifacts from run ID: ${env.GITHUB_RUN_ID}"

            // Bersihkan folder lama
            sh "rm -rf ${WORKSPACE}/allure-results"
            sh "mkdir -p ${WORKSPACE}/allure-results"

            // Cari artifact URL
            def artifactUrl = sh(
                script: """
                    curl -s -H "Accept: application/vnd.github+json" \
                        -H "Authorization: Bearer ${GITHUB_TOKEN}" \
                        "${GITHUB_API_URL}/actions/runs/${env.GITHUB_RUN_ID}/artifacts" \
                        | jq -r '.artifacts[] | select(.name | startswith("allure-results")) | .archive_download_url' \
                        | head -1
                """,
                returnStdout: true
            ).trim()

            echo "Artifact URL: ${artifactUrl}"

            if (artifactUrl && artifactUrl != "null") {
                // Download artifact
                sh """
                    curl -L \
                        -H "Authorization: Bearer ${GITHUB_TOKEN}" \
                        "${artifactUrl}" \
                        -o ${WORKSPACE}/allure-results.zip
                """

                // Cek ukuran file
                sh "ls -la ${WORKSPACE}/allure-results.zip"

                // Extract zip ke folder yang benar
                sh """
                    unzip -o ${WORKSPACE}/allure-results.zip -d ${WORKSPACE}/allure-results-temp/
                    cp -r ${WORKSPACE}/allure-results-temp/*/* ${WORKSPACE}/allure-results/ 2>/dev/null || true
                    cp -r ${WORKSPACE}/allure-results-temp/* ${WORKSPACE}/allure-results/ 2>/dev/null || true
                """

                // Hapus file zip
                sh "rm -f ${WORKSPACE}/allure-results.zip"
                sh "rm -rf ${WORKSPACE}/allure-results-temp"
            } else {
                echo "⚠️ No artifacts found"
            }

            // Debug: lihat isi folder
            sh """
                echo "=== Isi folder allure-results ==="
                ls -la ${WORKSPACE}/allure-results/
                echo "=== Jumlah file ==="
                find ${WORKSPACE}/allure-results -type f | wc -l
            """
        }
    }
}

    post {
        always {
            script {
                echo "🧹 Cleaning up workspace..."
                // Optional: archive results untuk debugging
            }
        }

        success {
            echo "🎉 Pipeline completed successfully!"
        }

        failure {
            echo "❌ Pipeline failed, but Allure report might still be available"
        }
    }
}
